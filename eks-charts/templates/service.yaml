apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-charts.fullname" . }}
  labels:
    {{- include "eks-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-charts.selectorLabels" . | nindent 4 }}
