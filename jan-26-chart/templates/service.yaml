apiVersion: v1
kind: Service
metadata:
  name: {{ include "jan-26-chart.fullname" . }}
  labels:
    {{- include "jan-26-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jan-26-chart.selectorLabels" . | nindent 4 }}
